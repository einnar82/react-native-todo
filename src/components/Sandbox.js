import React from 'react';
import { StyleSheet, Text, View } from 'react-native'

const Sandbox = () => {

}

const styles = new StyleSheet({
    "redis": {
        "port": "5001",
        "host": "http://localhost",
        "keyPrefix": "petnet_database_"
    }
});

export default Sandbox;